{
  "parameterConfig": {
    "parameters": [
      { "legalValue": ["dev","prod"], "defaultValue": "dev", "description": "env" },
      { "legalValue": ["uk","de"],    "defaultValue": "uk",  "description": "country" }
    ]
  },
  "xmlSchemaPath": "schemas/config.xsd"
}
