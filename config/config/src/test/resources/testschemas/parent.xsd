<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">
    <!-- Relative include should resolve under testschemas/ via systemId -->
    <xs:include schemaLocation="nested/child.xsd"/>
    <xs:element name="parent">
        <xs:complexType>
            <xs:sequence>
                <xs:element ref="child"/>
            </xs:sequence>
        </xs:complexType>
    </xs:element>
</xs:schema>
