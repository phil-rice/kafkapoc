module:
  notification:
    signedFor:
      logic:
        - type: celInline
          inline: |
            input.eventType == "signedFor"
              ? [{
                  type: "notification",
                  channel: "email",
                  to: "both",
                  message: "International parcel delivered and signed for in DE."
                }]
              : null
