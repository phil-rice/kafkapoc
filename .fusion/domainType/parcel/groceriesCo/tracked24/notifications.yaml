module:
  notification:
    signedFor:
      logic:
        - type: celInline
          inline: |
            input.eventType == "signedFor"
              ? [{
                  type: "notification",
                  channel: "email",
                  to: "both",
                  message: "Parcel delivered and signed for. Proof available in portal."
                }]
              : null
